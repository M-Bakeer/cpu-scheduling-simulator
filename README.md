# CPU Scheduling Simulator

A Python simulator for four CPU scheduling algorithms: FCFS, SJF, Priority, and Round Robin.

## How to run
python main.py

## Output
Gantt chart + per-process metrics (CT, TAT, WT, RT) and averages for each algorithm on two datasets.

## Requirements
Python 3 — no external libraries needed.
